{
  "name": "@seannyphoenix/binarytime",
  "version": "0.1.0",
  "description": "Binary Time, a new way of measuring and telling time",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "build:types": "tsc",
    "build:all": "npm run build && npm run build:types"
  },
  "keywords": [],
  "author": "Seanny Drakon Phoenix",
  "license": "ISC",
  "packageManager": "pnpm@10.32.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "esbuild": "^0.25.12",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}